# set RETICULATE_PYTHON according to your python installation of your ShinyServer.

if (!Sys.info()[['sysname']] == 'Windows'){
  # running on shinyapps.io, set the RETICULATE_PYTHON evironment variable accordingly
  Sys.setenv(RETICULATE_PYTHON = '/home/shiny/.virtualenvs/python35_env/bin/python')
} else
{
  #Running on my windows machine.
  Sys.setenv(RETICULATE_PYTHON = "C:\\Users\\DELL\\Anaconda3\\python.exe") 
}
